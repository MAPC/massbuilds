import discreteMap from 'massbuilds/utils/discrete-map';
import { module, test } from 'qunit';

module('Unit | Utility | discrete map');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = discreteMap();
  assert.ok(result);
});
