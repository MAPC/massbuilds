{{#if session.isAuthenticated}}
  {{removePanelParam}}
  {{account-subpanel}}
{{else}}
  {{#if (eq panel 'signup')}}
    {{signup-subpanel}}
  {{else}}
    {{login-subpanel 
      showSignupPanel=showSignupPanel
    }}
  {{/if}}
{{/if}}
