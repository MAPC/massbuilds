{{#if session.isAuthenticated}}
  {{account-subpanel}}
{{else}}
  {{#if (eq panel 'signup')}}
    {{signup-subpanel}}
  {{else}}
    {{login-subpanel 
      showSignupPanel=showSignupPanel
    }}
  {{/if}}
{{/if}}
